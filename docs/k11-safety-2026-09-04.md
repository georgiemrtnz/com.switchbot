# K11+ safety and room-cleaning implementation — 2026-09-04

## Installed and verified

- Installed the K11+-only monitor and guarded room-action additions to SwitchBot 2.0.87 without a clean install or re-pairing. All 71 SwitchBot IDs are unchanged and all 71 were available without warnings after deployment.
- Preserved the September 3 Start/Pause/Dock fixes and the previously deployed cumulative-energy changes. The unrelated global fan label edit remains in source but was excluded from the installed candidate.
- New visible device readings: Last valid status (UTC), Status age in minutes, Safety monitor. Receipt time updates even when the robot status is unchanged. This is the time Homey received a valid status, not proof of a physical inspection.
- Invalid, more-than-five-minute-old, future, and out-of-order timestamped webhook reports are excluded from monitoring and existing status updates. Status polling is single-flight and bounded to five minutes idle / one minute active. The timer is disposed on device shutdown.
- Persistent incident deduplication: reported trouble; reported offline for five minutes; missing updates during an unfinished run; no dock report ten minutes after reported return; unfinished run after two hours; no cleaning report two minutes after a room-start request. A pause or standby is never announced as completion. Dock reports close the unfinished session but do not assert that cleaning completed.
- Alerts go through the K11 attention trigger to a native Homey push notification for George and the Homey timeline. One explicitly labeled setup test reached the timeline at 2026-09-04T12:55:47.943Z. Phone receipt remains user-unconfirmed. Dedup is saved before delivery, so a failed delivery is not repeatedly retried; monitor alerts require the SwitchBot app and Homey to remain running.

## Living-room route

The native SwitchBot `Clean Living Room` scene was inspected and saved with only Living room selected on the room map. The existing MAX-suction action was preserved. Its start action is vacuuming, not mopping. It was not executed.

- Native scene ID: `8d83d6dd-c20a-4caf-8a18-e6dde04c7f70`
- Homey paired scene ID: `e202768a-5c84-4284-bcf6-3a175af056b8`
- Vacuum Homey ID: `6ddd0453-a0fe-4492-881b-c04b783711c5`
- Existing Advanced Flow `Clean Livingroom`: `8a0248a1-2e71-4370-a251-300351a08ced`

The Advanced Flow was updated in place, preserving its ID and both card IDs. Its previously disconnected Start block now links to the guarded native room-scene action instead of generic whole-home cleaning. It is enabled and unbroken. The old structure is saved in `k11-livingroom-flow-before-2026-09-04.json`; the replacement is in `k11-livingroom-flow-2026-09-04.json`.

The guarded action requires a freshly successful status read, explicit online status, an idle/docked robot, no tracked unfinished session, and at least 30% reported battery. Concurrent/double-click requests are blocked. Scene execution requires explicit SwitchBot acceptance, with no generic Start fallback and no automatic retry on ambiguous responses. Acceptance does not prove movement or room routing. Changing the native scene later requires re-verification; the API does not expose its room map to this guard.

## Scheduling and remaining gates

- Attention Flow `e79d6231-63b2-4001-9466-a0692c0d30dc`: enabled, unbroken.
- Daily draft Flow `986d7814-376d-4206-ab8a-56fa465f9613`: **disabled**, unbroken. Noon is only a placeholder, not an approved schedule.
- Device `k11_room_verified=true` reflects scene configuration inspection, not physical testing.
- `k11_test_verified=false`, `k11_schedule_armed=false`.
- Quiet-hour defaults are 22:00–09:00, using Homey's local timezone; user approval/preferences are pending.
- Scheduled action also checks test/arming flags, quiet hours, and a persisted once-per-local-day reservation saved before command submission. Rejection or uncertain acceptance does not release that day's reservation. Manual room starts are not subject to the daily quota or quiet hours; the schedule never requests mopping.
- Live execution of the **disarmed scheduled action** correctly returned: `Automatic cleaning is disabled pending the supervised test and schedule approval`. It sent no scene/cleaning command.

Still required: user present with a clear floor; supervised Start → Pause → Dock and living-room-routing observation; confirmation of the notification on the phone; desired daily time and quiet hours. Only then set the physical-test flag, arm the device schedule, set the agreed time, and enable the daily Flow. No cleaning, pause, dock, suction, volume, or mopping command was sent during this implementation.

## Verification and recovery

- 60/60 unit tests passed in both source and isolated candidate.
- Verified-level Homey validation passed; ESLint passed on changed runtime JavaScript with the pre-existing line-ending rule disabled; `git diff --check` passed.
- Live Homey browser rendered the three new monitoring readings; Charge Done, Stand By, and 100% battery remained visible.
- The installed manifest matches the generated source manifest except for the intentionally excluded pre-existing global fan label edit.
- Isolated prior baseline: `/tmp/switchbot-k11-safety.0TWm0P/baseline`; installed candidate: `/tmp/switchbot-k11-safety.0TWm0P/candidate`. These are temporary, private staging paths, not permanent backups. Credentials were preserved privately and never printed.
- Install from the candidate directory using `homey app install`, not `--path` from a different working directory.
- To undo only the Flow route, update the existing Advanced Flow ID with the saved before JSON. Do not delete/recreate the paired robot or restore unrelated Flows. Disable the new alert Flow if rolling back the driver. Keep the schedule disarmed and disabled until physical verification is complete.

Sources used for API behavior: [SwitchBot K11+ API](https://github.com/OpenWonderLabs/SwitchBotAPI/blob/main/devices/robot-vacuum/robot-vacuum-k11.md), [SwitchBot scene API](https://github.com/OpenWonderLabs/SwitchBotAPI), [Homey notifications](https://apps-sdk-v3.developer.homey.app/ManagerNotifications.html).
